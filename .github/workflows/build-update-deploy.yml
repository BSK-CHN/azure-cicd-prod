name: Build, Push & Update Deployment Repo (AKS + ArgoCD demo)

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-update:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout app repo (azure-cicd-prod)
      - name: Checkout app repo
        uses: actions/checkout@v4

      # Step 2: Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Step 3: Login to DockerHub
      - name: Docker login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # Step 4: Build Docker image
      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/aks-demo:${{ github.sha }} .

      # Step 5: Push Docker image
      - name: Push Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/aks-demo:${{ github.sha }}

      # Step 6: Checkout deployment repository (azure-cicd-deploy)
      - name: Checkout deployment repo
        uses: actions/checkout@v4
        with:
          repository: kaniskaa/azure-cicd-deploy   # 🔹 Replace with your actual GitHub username or org name
          token: ${{ secrets.DEPLOY_REPO_TOKEN }}
          path: deploy-repo

      # (Optional) Step 7: Debug - List files to verify correct structure
      - name: List files in deployment repo
        run: |
          echo "Deployment repo structure:"
          ls -R deploy-repo

      # Step 8: Update image tag in deployment manifest
      - name: Update image tag in deployment manifest
        run: |
          sed -i "s|image: .*|image: ${{ secrets.DOCKERHUB_USERNAME }}/aks-demo:${{ github.sha }}|" deploy-repo/cicd-new-deploy/deployment.yaml

      # Step 9: Commit and push manifest update
      - name: Commit and push manifest update
        run: |
          cd deploy-repo
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add cicd-new-deploy/deployment.yaml
          git commit -m "ci: update image to ${{ github.sha }}" || echo "No changes to commit"
          git push
